# Legal content synchronization

Run `npm run legal:sync` after changing the canonical pack in `NutriTrackBeta/web/getnutria_gdpr_starter_pack_v1_0`. The landing build runs this command automatically and contains only the seven approved public documents. Never place internal policy, ROPA, DPIA, breach or retention documents in `content/legal` or `public`.
